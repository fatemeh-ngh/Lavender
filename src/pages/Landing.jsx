import React from 'react';

//components
import Navbar from '../components/Navbar';

const Landing = () => {
    return (
        <div>
            <Navbar/>
        </div>
    );
};

export default Landing;